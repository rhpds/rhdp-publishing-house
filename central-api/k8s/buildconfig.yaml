apiVersion: v1
kind: BuildConfig
metadata:
  name: central-api
  namespace: publishing-house
  labels:
    app: central-api
spec:
  output:
    to:
      kind: ImageStreamTag
      name: central-api:latest
  source:
    type: Git
    git:
      uri: https://github.com/treddy08/rhdh-publishing-house-central.git
      ref: main
    contextDir: central-api
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  triggers:
  - type: ConfigChange
---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: central-api
  namespace: publishing-house
  labels:
    app: central-api
spec:
  lookupPolicy:
    local: true

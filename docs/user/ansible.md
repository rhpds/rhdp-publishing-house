# Ansible

> **Note:** Publishing House provides an `ansible-helper` skill that automates
> the Ansible workload setup for you. If you prefer to set it up manually,
> follow the structure below.

## Overview

This guide covers how to structure and write Ansible workloads for RHDP labs.

<!-- Mitesh to document the following sections:

1. Project structure - directory layout for an AgnosticD-compatible workload
2. Writing roles - naming conventions, tasks/main.yml patterns, defaults
3. Required variables - what PH passes in, what the workload must define
4. Local testing - how to run the workload against a sandbox before submitting
5. CI integration - how the workload gets picked up by AgnosticD v2

-->

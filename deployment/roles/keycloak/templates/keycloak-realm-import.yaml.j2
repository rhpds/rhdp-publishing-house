apiVersion: k8s.keycloak.org/v2alpha1
kind: KeycloakRealmImport
metadata:
  name: "{{ keycloak_realm }}"
  namespace: "{{ keycloak_namespace }}"
spec:
  keycloakCRName: keycloak
  realm: {{ lookup('template', 'realm-import.json.j2') | from_json | to_json }}

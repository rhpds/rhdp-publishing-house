apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhbk-operator
  namespace: "{{ keycloak_namespace }}"
spec:
  channel: "{{ keycloak_operator_channel }}"
  name: rhbk-operator
  source: "{{ keycloak_operator_source }}"
  sourceNamespace: openshift-marketplace
  installPlanApproval: Automatic

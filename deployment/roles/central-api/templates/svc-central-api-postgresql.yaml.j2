apiVersion: v1
kind: Service
metadata:
  name: central-api-postgresql
  namespace: "{{ app_namespace }}"
  labels:
    app: central-api-postgresql
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: central-api-postgresql
  clusterIP: None
